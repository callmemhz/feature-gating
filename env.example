MONGO_URL=mongodb://localhost:27017/wawa-fg
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
JWT_SECRET_KEY=$(openssl rand -hex 32 | head -c 64)
CACHE_TTL_SECONDS=60
APP_TITLE=Feature Gating
# 机器调用用的 API Key（逗号分隔，每项 "secret" 或 "label:secret"）。
# 带 X-API-Key 头请求即等同 admin，可读写 projects/snapshots。留空则关闭。
API_KEYS=